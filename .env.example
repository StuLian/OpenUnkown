# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 忽略，不要提交到仓库）
#   cp .env.example .env
#
# ===== LangSmith 追踪 =====
# 在 https://smith.langchain.com 创建 API Key（个人/组织均可）后粘贴到下面
LANGSMITH_API_KEY=lsv2_pt_your_api_key_here

# 开启追踪（必须为 true 才会自动上报）
LANGSMITH_TRACING=true

# 追踪数据写入的项目名（可选，留空则默认使用应用名 "OpenUnknown"）
# LANGSMITH_PROJECT=OpenUnknown

# 自托管（self-hosted）LangSmith 才需要配置接入地址，SaaS 云端无需设置
# LANGSMITH_ENDPOINT=http://your-langsmith-host:1984

# ===== 模型服务（可选） =====
# 本项目默认通过 ~/.dashscope/api_key 读取 DashScope Key；
# 也可用环境变量覆盖（会被 config.py 的 get_api_key 读取）。
# DASHSCOPE_API_KEY=sk-your-dashscope-key
